Ext.define('{appName}.controller.{name}', {
    extend: 'Ext.app.Controller',
    
    config: {
        refs: {
            
        {[ "\}"]},
        control: {
            
        {[ "\}"]}
    {[ "\}"]},
    
    //called when the Application is launched, remove if not needed
    launch: function(app) {
        
    {[ "\}"]}
});